package TAD_Interfețe_Iterator;

public class ParcurgereaElementelorCuIterator {
	 /* Iterator iteratorNou = new Iterator<>();						//Declarăm noul iterator:
	 * 	iteratorNou = numeContinerPesteCareIterăm.iterator();			//Apelăm funcția creeazăIterator.
	 * 	
	 * 	while(iteratorNou.valid()){										//Parcurgem continerul.
	 * 		TElement elementCurent;
	 * 		elementCurent = iteratorNou.element();						//Acesăm elementul curent.
	 * 		
	 * 		//facem ceva cu elementul curent (e.g. afișăm).	
	 * 		
	 * 		iteratorNou.următor();										//Trecem la următorul element.
	 * 	}
	 */
	
	/*Parcurgerea cu un iterator se face în acest mod indiferent de 
	 * containerul exact și 
	 * reprezentarea containerului!
	 */
	
	
}
